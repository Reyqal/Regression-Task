# Cara Setup Project:
1. Berdoa
2. Jalankan di terminal kamu:
   git clone https://github.com/Reyqal/Regression-Task.git
3. buat venv & install library, jalankan di terminal:
    - py -m venv venv
    - pip install -r requirements.txt